import { IntersectionRoot } from './intersection-root';

describe('IntersectionRoot', () => {
  it('should create an instance', () => {
    const directive = new IntersectionRoot();
    expect(directive).toBeTruthy();
  });
});
